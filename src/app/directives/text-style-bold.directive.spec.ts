import { TextStyleBoldDirective } from './text-style-bold.directive';

describe('TextStyleBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new TextStyleBoldDirective();
    expect(directive).toBeTruthy();
  });
});
